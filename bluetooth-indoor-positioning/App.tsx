import 'react-native-gesture-handler';
import { View } from 'react-native';

export default function App() {
  return (
    <View style={{ flex: 1 }}>
      {/* Expo Router handles the app rendering */}
    </View>
  );
}
